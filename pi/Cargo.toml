[package]
name = "pi"
version = "0.1.0"
authors = ["Sergio Benitez <sb@sergio.bz>"]
edition = "2018"

[dependencies]
volatile = { path = "../volatile" }

[target.'cfg(target_os = "ros")'.dependencies]
std = { path = "../../os/std" }
